<tr>
  <td><%= link_to post.title, post_path(post) %></td>
  <td></td>
  <td><%= link_to 'Edit', edit_admin_post_path(post), class: 'btn btn-sm btn-warning' %></td>
  <td><%= link_to 'Remove', admin_post_path(post), method: :delete, data: {
    confirm: 'Confirm delete action!'}, class: 'btn btn-sm btn-danger' %></td>
<tr>