<div class="c-show-user">

  <h1><%= @user.username %>'s Page </h1><hr>

  <div class="c-show-user__email">
    <h3><%= @user.email %></h3>
  </div>

  <div class="c-show-user__avatar">
    <p><%= image_tag avatar_url(@user) %></p>
  </div>

  <h4><%= @user.username %>'s Posts </h4>

</div>


<%= will_paginate @user_posts %>

<div class="l-posts-container">
  <%= render partial: "shared/post", collection: @user.posts %>
</div>


<%= will_paginate @user_posts %>
