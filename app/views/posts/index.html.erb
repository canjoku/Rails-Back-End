<% provide(:page_title, 'All Posts') %>

<div>
  <%= will_paginate @posts %>
</div>

<div class="b-posts-container">
  <%= render partial: "shared/post", collection: @posts %>
</div>

<div>
  <%= will_paginate @posts %>
</div>
